# Hyperparameter grid with gradient recording

This is just a redo of [the hyperparameter grid experiment](./19-hyperparameter-grid.md) with gradient recording enabled. Mainpurpose of having a separate experiment is to keep the old and new data separate.

